import { CSSProperties } from 'react'

export const snippets = {
  links: {
    color: 'white',
    textDecoration: 'underline',
    whiteSpace: 'nowrap',

    '&:hover': {
      color: 'white',
    },
  } as CSSProperties,
}
